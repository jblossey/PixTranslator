/* eslint-disable no-undef */
/*
test('getDeeplTranslationsForOneSpliceOk', () => {
	jest.mock(needle);
	needle.mockResolvedValue({
		statusCode: 200,
		body: {
			translations: null
		}
	});
}); */

test('sample', () => {});
